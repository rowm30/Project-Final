import { BannerShadowDirective } from './banner-shadow.directive';

describe('BannerShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerShadowDirective();
    expect(directive).toBeTruthy();
  });
});
