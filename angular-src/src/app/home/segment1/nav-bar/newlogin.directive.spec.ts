import { NewloginDirective } from './newlogin.directive';

describe('NewloginDirective', () => {
  it('should create an instance', () => {
    const directive = new NewloginDirective();
    expect(directive).toBeTruthy();
  });
});
