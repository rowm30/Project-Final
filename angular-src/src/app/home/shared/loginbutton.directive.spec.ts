import { LoginbuttonDirective } from './loginbutton.directive';

describe('LoginbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
